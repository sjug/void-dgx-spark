# Template file for 'nvidia-kernel-defaults'
# 1:1 with Ubuntu package of the same name
pkgname=nvidia-kernel-defaults
version=21.05
revision=1
short_desc="NVIDIA sysctl default kernel settings for DGX"
maintainer="Void DGX Spark <void-dgx-spark@example.com>"
license="custom:NVIDIA"
homepage="https://www.nvidia.com/"
create_wrksrc=yes
archs="aarch64"

do_install() {
	vinstall ${FILESDIR}/20-nvidia-defaults.conf 644 usr/lib/sysctl.d
}
